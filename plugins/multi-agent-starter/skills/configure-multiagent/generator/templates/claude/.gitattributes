# 이 시스템은 POSIX 우선이다(bash 디스패처 _shared/adapters/call_worker.sh).
# 워킹트리 개행을 플랫폼과 무관하게 LF로 고정한다 — Windows(core.autocrlf=true)에서
# CRLF로 체크아웃되면 셸 스크립트와 바이트 단위 비교가 깨진다.
* text=auto eol=lf

# 바이너리 자산은 개행 변환 금지
*.png binary
